# Test Data Sets

All test data sets are stores in ``/documents/testdata``. That folder contains subfolders for each test data set. 

Each test data set contans a description file which describes the test data set.